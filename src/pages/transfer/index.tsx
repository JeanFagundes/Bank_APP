import TransferComponent from 'components/ListFinance/TransferComponent';
import styles from './Transfer.module.scss';

export default function Transfer() {
    return (
        <div className={styles.container}>
            <TransferComponent />
        </div>
    );
}
